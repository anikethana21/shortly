# ==============================================================================
# TEMPLATE ONLY — values are placeholders, not real secrets.
# In production, use a sealed-secrets or external-secrets solution.
# kubectl create secret generic shortly-secrets -n shortly \
#   --from-literal=MONGO_URL='...' \
#   --from-literal=REDIS_URL='...' \
#   --from-literal=KAFKA_BOOTSTRAP_SERVERS='...' \
#   --from-literal=KAFKA_USERNAME='...' \
#   --from-literal=KAFKA_PASSWORD='...' \
#   --from-literal=BASE_URL='https://your-domain.com'
# ==============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: shortly-secrets
  namespace: shortly
type: Opaque
stringData:
  MONGO_URL: "REPLACE_ME"
  REDIS_URL: "REPLACE_ME"
  KAFKA_BOOTSTRAP_SERVERS: "REPLACE_ME"
  KAFKA_USERNAME: "REPLACE_ME"
  KAFKA_PASSWORD: "REPLACE_ME"
  BASE_URL: "REPLACE_ME"
